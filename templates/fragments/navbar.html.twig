<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home') }}">Symrecipe</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    {% if app.user != null %}
      <div class="collapse navbar-collapse justify-content-around" id="navbarColor01">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link" href="{{ path('ingredients') }}">Ingrédients</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('recipes') }}">Recettes</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('ingredient.new') }}">Créer ingrédient</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('recipe.new') }}">Créer recette</a>
          </li>
        </ul>
        {# <form class="d-flex">
          <input class="form-control me-sm-2" type="text" placeholder="Search">
          <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
        </form> #}
      {% endif %}

        <div class="d-flex">
            <ul class="navbar-nav">
              {% if app.user %}
                <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.fullname }}</a>
                  <div class="dropdown-menu">
                    <a class="dropdown-item" href="{{ path('user.edit', {id : app.user.id}) }}">Mes Informations</a>
                    <a class="dropdown-item" href="{{ path('user.password', {id : app.user.id}) }}">Mot de passe</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('security.logout') }}">Déconnexion</a>
                  </div>
                </li>
              {% else %}
                <li class="nav-item">
                  <div>
                    <a class="text-dark mx-3" href="{{ path('security.register') }}">Inscription</a>
                    <a class="text-dark mx-3" href="{{ path('security.login') }}">Connexion</a>
                  </div>
                </li>
              {% endif %}
          </ul>
        </div>
      </div>
  </div>
</nav>